
volumes:
  etc:
  home:
  usr_local:
  var_lib_containers:

services:
  faasd:
    runtime: sysbox-runc
    hostname: faasd-${FAASD_INSTANCE:-default}-1
    build:
      context: faasd
    restart: unless-stopped
