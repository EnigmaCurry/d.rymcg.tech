## d-rymcg-tech image builder (GitHub Actions)
## Builds the Docker image from the master branch and pushes to ghcr.io.
## No secrets configuration needed — uses the built-in GITHUB_TOKEN.

name: Build d-rymcg-tech image

on:
  push:
    branches: [master]
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare image metadata
        id: meta
        run: |
          SHA=$(echo "${{ github.sha }}" | head -c 12)
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "owner=$OWNER" >> "$GITHUB_OUTPUT"

      - name: Check if image already exists
        id: check
        run: |
          if docker buildx imagetools inspect "ghcr.io/${{ steps.meta.outputs.owner }}/d-rymcg-tech:${{ steps.meta.outputs.sha }}" 2>/dev/null; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          fi

      - uses: docker/setup-qemu-action@v3
        if: steps.check.outputs.exists != 'true'

      - uses: docker/setup-buildx-action@v3
        if: steps.check.outputs.exists != 'true'

      - uses: docker/build-push-action@v6
        if: steps.check.outputs.exists != 'true'
        with:
          context: .
          file: _container/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            BRANCH=master
            IMAGE_SOURCE=https://github.com/${{ github.repository }}
          tags: |
            ghcr.io/${{ steps.meta.outputs.owner }}/d-rymcg-tech:${{ steps.meta.outputs.sha }}
            ghcr.io/${{ steps.meta.outputs.owner }}/d-rymcg-tech:latest
          cache-from: type=registry,ref=ghcr.io/${{ steps.meta.outputs.owner }}/d-rymcg-tech:latest
          cache-to: type=inline
