postgres:
  host:     ${COMMENTARIO_POSTGRES_HOST}
  port:     ${COMMENTARIO_POSTGRES_PORT}
  database: ${COMMENTARIO_POSTGRES_DB}
  username: ${COMMENTARIO_POSTGRES_USER}
  password: ${COMMENTARIO_POSTGRES_PASSWORD}
smtpServer:
  host:     ${COMMENTARIO_SMTP_HOST}
  port:     ${COMMENTARIO_SMTP_PORT}
  username: ${COMMENTARIO_SMTP_USERNAME}
  password: ${COMMENTARIO_SMTP_PASSWORD}
  
