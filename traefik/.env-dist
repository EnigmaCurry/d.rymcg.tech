## Enter your email address for Let's Encrypt:
ACME_CA_EMAIL=you@example.com

## Enter htpasswd encoded username/password for accessing the dashboard
## Run `make config` to automatically fill this in
TRAEFIK_DASHBOARD_AUTH=

## Traefik docker image:
TRAEFIK_IMAGE=traefik:v2.9
## Log levels: error, warn, info, debug
TRAEFIK_LOG_LEVEL=info

## You can opt-in to sending feeback to Traefik Labs
## (Default is false [OFF]; unless you uncomment this next line):
#TRAEFIK_SEND_ANONYMOUS_USAGE=true

## Enable access logs or not:
TRAEFIK_ACCESS_LOGS_ENABLED=false
TRAEFIK_ACCESS_LOGS_PATH=/data/access.log

## The subnet of the proxy peer network:
TRAEFIK_PROXY_SUBNET=
## The IP address of traefik on the traefik-proxy network:
TRAEFIK_PROXY_SUBNET_IP=

## The subnet of the traefik-wireguard network:
TRAEFIK_WIREGUARD_SUBNET=
## The IP address of traefik on the traefik-wireguard network:
TRAEFIK_WIREGUARD_SUBNET_IP=

## The subnet of the traefik-mail network:
TRAEFIK_MAIL_SUBNET=
## The IP address of traefik on the traefik-wireguard network:
TRAEFIK_MAIL_SUBNET_IP=

## ACME Challenge (Let's Encrypt)
## These are ignored if you are not using DNS-01 challenge type.
## Set the variable for your own DNS provider:
##   https://doc.traefik.io/traefik/https/acme/#providers
## Example of DO_AUTH_TOKEN is appropriate for DigitalOcean DNS only:
TRAEFIK_ACME_DNS_PROVIDER=digitalocean
TRAEFIK_ACME_DNS_VARNAME=DO_AUTH_TOKEN
DO_AUTH_TOKEN=
