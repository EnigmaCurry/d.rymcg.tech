# The domain name for the whoami service:
WHOAMI_TRAEFIK_HOST=whoami.d.rymcg.tech

# The traefik certificate resolver:
# Can be production, staging, or acme-dns.
ACME_CERT_RESOLVER=production
# The root domain for the wildcard certificate:
# (Only used if ACME_CERT_RESOLVER=acme-dns):
ROOT_DOMAIN=d.rymcg.tech

