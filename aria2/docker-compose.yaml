services:
  aria2:
    build:
      context: ${ARIA2_GIT_REPO}#${ARIA2_GIT_BRANCH}
      args:
        UID: ${ARIA2_UID}
        GID: ${ARIA2_GID}
    environment:
      - RPC_SECRET=${ARIA2_RPC_SECRET}
      - WEB_PORT=8089
      - RPC_PORT=6801
    volumes:
      - ${ARIA2_DOWNLOAD_VOLUME}:/downloads:z

