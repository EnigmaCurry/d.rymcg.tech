version: "3.9"

services:
  app:
    build:
      context: .
    security_opt:
      - no-new-privileges:true
    environment:
      DEV_CONSOLE: "true"
    # Labels are defined in docker-compose.instance.yaml
    labels: []
