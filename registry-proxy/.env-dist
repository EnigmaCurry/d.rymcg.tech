# The docker image to use:
REGISTRY_PROXY_IMAGE=rpardini/docker-registry-proxy:0.6.5

# The name of this instance. If there is only one instance, use 'default'.
REGISTRY_PROXY_INSTANCE=
# You can protect this instance from uninstallation by setting this true:
## If true, make this instance immune to: `make uninstall`, `make destroy`
## Still allowed regardless: `make install`, `make stop`, `make start`, `make restart`
REGISTRY_PROXY_INSTANCE_PROTECTION=false

# Filter access by IP address source range (CIDR):
##Disallow all access: 0.0.0.0/32
##Allow all access: 0.0.0.0/0
REGISTRY_PROXY_IP_SOURCERANGE=0.0.0.0/0

# Additional registries to cache (space-separated).
# Docker Hub is always included automatically.
REGISTRY_PROXY_REGISTRIES=k8s.gcr.io gcr.io quay.io ghcr.io

# Authentication for upstream registries (space-separated).
# Format: hostname:username:password
# For Docker Hub use auth.docker.io as the hostname.
# Example: auth.docker.io:myuser:mypass ghcr.io:myuser:mytoken
REGISTRY_PROXY_AUTH_REGISTRIES=

# Enable manifest caching:
REGISTRY_PROXY_ENABLE_MANIFEST_CACHE=true

# Maximum cache size (nginx size syntax):
REGISTRY_PROXY_CACHE_MAX_SIZE=32g

# META:
# PREFIX=REGISTRY_PROXY
