## DO NOT EDIT - This Docker Compose override file is generated from the docker-compose.instance.yaml template.
## This file is automatically recreated whenever you run `make config` or `make install`.
services:
  wp:
    labels:
    - traefik.enable=true
    - traefik.http.routers.wp-${WP_INSTANCE}-wp.rule=Host(`${WP_TRAEFIK_HOST}`)
    - traefik.http.routers.wp-${WP_INSTANCE}-wp.entrypoints=websecure
    - traefik.http.middlewares.wp-${WP_INSTANCE}-wp-ipwhitelist.ipwhitelist.sourcerange=${WP_IP_SOURCERANGE}
    - traefik.http.routers.wp-${WP_INSTANCE}-wp.middlewares=wp-${WP_INSTANCE}-wp-ipwhitelist
