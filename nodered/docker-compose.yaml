version: "3.9"

volumes:
  node-red:

services:
  nodered:
    image: "nodered/node-red"
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    labels: []
    volumes:
      - node-red:/data
