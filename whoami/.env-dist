# The domain name for the whoami service:
WHOAMI_TRAEFIK_HOST=whoami.example.com

# Filter access by IP address source range (CIDR):
##Disallow all access:
#IP_SOURCERANGE="0.0.0.0/32"
##Allow all access:
IP_SOURCERANGE="0.0.0.0/0"
