# The name of this instance. If there is only one instance, use 'default'.
TOR_INSTANCE=

# JSON list of hidden services:
# Each entry creates a Tor hidden service.
#
# HTTP services via nginx proxy (recommended):
#   make add-service svc=whoami host=whoami.example.com
#   An nginx container proxies Tor port 80 to Traefik's websecure
#   entrypoint (port 443) with the correct Host header and TLS SNI.
#   No reconfiguration of the target service is needed.
#
# HTTP services via web_plain (alternative):
#   make add-service svc=whoami
#   Routes Tor port 80 to Traefik web_plain entrypoint (port 8000).
#   The target service must be configured with TRAEFIK_HOST=<onion address>
#   and TRAEFIK_ENTRYPOINT=web_plain.
#
# TCP services (name + port mapping):
#   make add-service svc=irc port=6667:6697
#   Routes a Tor TCP port to any port on localhost.
#   This can be a Traefik TCP entrypoint or any local service (e.g., SSH).
#
#TOR_HIDDEN_SERVICES=["whoami"]
#TOR_HIDDEN_SERVICES=[["whoami", "whoami.example.com"]]
#TOR_HIDDEN_SERVICES=[["irc", 6667, 6697]]
TOR_HIDDEN_SERVICES=[]

# Base port for nginx reverse proxy listeners.
# Each 2-element service gets a sequential port starting from this value.
TOR_NGINX_PORT_BASE=10080

# META:
# PREFIX=TOR
