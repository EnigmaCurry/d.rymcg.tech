# The name of this instance. If there is only one instance, use 'default'.
TOR_INSTANCE=

# JSON list of hidden services:
# Each entry creates a Tor hidden service.
#
# HTTP services (2-element entries):
#   ["name", "traefik-docker-service"]
#   Routes Tor port 80 to Traefik web_plain entrypoint (port 8000).
#   The traefik-docker-service follows the pattern: {project}-{instance}-{service}
#   Example: whoami instance "default" -> whoami-default-whoami
#
# TCP services (3-element entries):
#   ["name", tor_port, traefik_port]
#   Routes a Tor TCP port to a Traefik TCP entrypoint on localhost.
#   The service must already be configured with the matching entrypoint.
#   Example: inspircd on Traefik custom entrypoint port 6696 -> ["irc", 6667, 6696]
#
#TOR_HIDDEN_SERVICES=[["whoami", "whoami-default-whoami"]]
#TOR_HIDDEN_SERVICES=[["irc", 6667, 6696]]
TOR_HIDDEN_SERVICES=[]

# JSON map of .onion addresses: {"name": "abcdef...onion", ...}
# Leave as {} on first install. After install, run `make onion-addresses`
# to get the generated .onion addresses, then set them here and reinstall.
TOR_ONION_HOSTS={}

# META:
# PREFIX=TOR
