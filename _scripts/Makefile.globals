## This Makefile is included by all other Makefiles to set global targets.
## This include script expects ROOT_DIR to be set in the parent.

SHELL = /bin/bash
## Prefix name for image tags:
DOCKER_ORG ?= localhost
## Tools path:
BIN = $(shell realpath ${ROOT_DIR}/_scripts)

DOCKER_CONTEXT ?= $$(docker context ls --format "{{.Current}} {{.Name}}" | grep -oP "^true \K.*")

## Root environment file (contains ROOT_DOMAIN)
ROOT_ENV = .env_${DOCKER_CONTEXT}
## Per-project environment file name is keyed to the current docker context and the optional INSTANCE name:
instance ?= "$${INSTANCE:-default}"
INSTANCE = ${instance}
INSTANCE_SUFFIX ?= $$( (set +x; [[ "${instance}" == "default" ]] && echo "" || echo "_${instance}") )
INSTANCE_URL_SUFFIX ?= $$( (set +x; [[ "${instance}" == "default" ]] && echo "" || echo "-${instance}") )
CONTEXT_INSTANCE ?= "${DOCKER_CONTEXT}_${instance}"
ENV_FILE ?= ".env_${CONTEXT_INSTANCE}"
PROJECT_NAME ?= $$(basename $${PWD})${INSTANCE_SUFFIX}
CWD_PROJECT_NAME ?= $$(basename $${PWD})${INSTANCE_SUFFIX}
PROJECT_INSTANCE ?= ${CWD_PROJECT_INSTANCE}
DOCKER_COMPOSE_FILE_ARGS ?= $$( (set +x; [[ -f docker-compose.override${INSTANCE_SUFFIX}.yaml ]] && echo "-f docker-compose.yaml -f docker-compose.override_${CONTEXT_INSTANCE}.yaml" || echo "-f docker-compose.yaml") )

.PHONY: all
all: help

.PHONY: check-instance-project
check-instance-project:
	@[[ "${instance}" != "default" ]] && ([[ "${PROJECT_NAME}" == "${CWD_PROJECT_INSTANCE}" ]] || (echo -e "# The current sub-shell is locked to an instance: ${INSTANCE}\n# from the directory: $${PROJECT}\n# Press Ctrl-D to exit this sub-shell." && exit 1)) || true

.PHONY: check-in-subshell
check-in-subshell:
	@[[ "${instance}" != "default" ]] && ([[ -z "${INSTANCE_SUBSHELL}" ]] || (echo -e "# The current sub-shell is locked to an instance: ${INSTANCE}\n# from the directory: $${PROJECT}\n# Press Ctrl-D to exit this sub-shell." && exit 1)) || true

%-rule-exists:
	@$(MAKE) -n $* &> /dev/null
