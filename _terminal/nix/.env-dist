## A unique name for this instance:
NIX_INSTANCE=

## The generic name to prefix the instance name with:
NIX_GENERIC_PREFIX=nix

## The suffix to apply at the end of the instance name:
## This is used to specify the tty number:
NIX_INSTANCE_SUFFIX=

## Create public tags for each of the base images to build:
## (These images are tagged to be built for the public d.rymcg.tech community images,
##  which you won't be able to push to, [unless your name is EnigmaCurry],
##  if you want to push these images elsewhere, change the tag prefix accordingly,
##  or use `localhost/` if you're not going to push them anywhere else):
NIX_BASE_IMAGE_TAG=registry.digitalocean.com/rymcg-tech/nix-base:v0.0.1
NIX_COMMON_IMAGE_TAG=registry.digitalocean.com/rymcg-tech/nix-common:v0.0.1

## The NIX_USER_IMAGE_TAG should not normally be pushed to a public registry (so use localhost):
NIX_USER_IMAGE_TAG=localhost/${NIX_GENERIC_PREFIX}-${NIX_INSTANCE}-user:v1.0.0

## This is the user to create in the image:
NIX_USER_NAME=nix-user
NIX_USER_UID=1000
NIX_USER_GID=1000

NIX_HOMEMANAGER_VERSION=22.11

## Your git username and email:
NIX_GIT_USERNAME=
NIX_GIT_EMAIL=

## Git repository for d.rymcg.tech
NIX_GIT_D_RYMCG_TECH_CLONE=git/vendor/enigmacurry/d.rymcg.tech
NIX_GIT_D_RYMCG_TECH_REPO=https://github.com/EnigmaCurry/d.rymcg.tech.git
NIX_GIT_D_RYMCG_TECH_REF=master

NIX_DOCKER_SSH_HOST=
NIX_DOCKER_SSH_USER=root
NIX_DOCKER_SSH_PORT=22
